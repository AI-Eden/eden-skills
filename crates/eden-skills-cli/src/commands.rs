use eden_skills_core::error::EdenError;

pub fn plan(config_path: &str) -> Result<(), EdenError> {
    println!("plan: pending implementation (config: {config_path})");
    Ok(())
}

pub fn apply(config_path: &str) -> Result<(), EdenError> {
    println!("apply: pending implementation (config: {config_path})");
    Ok(())
}

pub fn doctor(config_path: &str) -> Result<(), EdenError> {
    println!("doctor: pending implementation (config: {config_path})");
    Ok(())
}

pub fn repair(config_path: &str) -> Result<(), EdenError> {
    println!("repair: pending implementation (config: {config_path})");
    Ok(())
}
